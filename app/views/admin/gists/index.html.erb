<div class="mt-4">
  <h1><%= t('.header')%></h1>
  <div class="row justify-content-center mt-3">
    <table class="table">

      <thead class="thead-dark">
        <tr>
          <th><%= t('.question') %></th>
          <th><%= t('.gist_url') %></th>
          <th><%= t('.user') %></th>
        </tr>
      </thead>

    <%= render @gists %>
    </table>
  </div>
</div>