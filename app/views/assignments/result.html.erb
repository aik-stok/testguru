<h1> Test "<%= @assignment.test.title %>" was completed.</h1>
<p>
  <h2>Result:<font color="
    <% if @assignment.percent_of < 85 %>
      <%= 'red' %>
      <% message = "ПОТРАЧЕНО" %>
    <% else %>
      <%= 'green' %>
      <% message = "Success" %>
    <% end %> ">
    <%= @assignment.percent_of %> % </font> of answers was right. <%= message %>.</h2>
</p>
<p><%= link_to "Back to Tests", tests_path %></p>